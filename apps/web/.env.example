# Server-side only. Never commit real secrets.

# AI provider
# Use "mock" for local architecture testing or "openai" for real generation.
AI_PROVIDER=mock
AI_API_KEY=
AI_MODEL=gpt-5.4-mini

# Database (reserved for the persistent Postgres/Supabase adapter)
DATABASE_URL=

# Future GitHub project export
GITHUB_TOKEN=
